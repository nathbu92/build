// Web stub for react-native-graph
import React from "react";
import { View } from "react-native";

export const LineGraph = ({ style }) => React.createElement(View, { style });
export const BarGraph = ({ style }) => React.createElement(View, { style });
export default { LineGraph, BarGraph };
